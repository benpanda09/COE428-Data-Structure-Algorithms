#include <stdlib.h>
#include <stdio.h>
#include "towers.h"

int main(int argc, char **argv)
{
    int n = 3;
    int from = 1;
    int dest = 2;
    int tempValue;
    
    if (argc == 2)
        n = atoi(argv[1]);
    else if(argc >= 3){
        n = atoi(argc[1]);
	from = atoi(argc[2]);
	tempValue = atoi(argc[3]);
	if(temp != from)
	  dest = tempValue;
	else{
	  fprintf(stderr,"Please Enter the Correct Input.");
	  exit(0);	
	  
    }
    towers(n, from, dest);
    exit(0);
}

